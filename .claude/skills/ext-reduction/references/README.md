# ext-reduction / references

Reduction primitives (reduce_sum / max / min / mean, incl. int8) go here — one YAML per
primitive, filled in the confidential env. Field contract identical to extension-guide
(name/semantics/signature/category/example/pitfalls + optional module/applies_to); see the
top-level `.claude/skills/extension-guide/references/README.md`.
