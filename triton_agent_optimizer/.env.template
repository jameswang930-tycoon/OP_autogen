# Triton Agent Optimizer — 环境变量配置
# 复制此文件为 .env 并填入你的真实值
#   cp .env.template .env

# LLM API Key (二选一即可)
# DeepSeek (推荐, OpenAI 兼容): https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
# Anthropic: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx

# msprof 模式 (自动检测, 也可手动指定)
# simulator = 使用 msprof op simulator (CPU仿真, 无需NPU)
# hardware  = 使用真机 msprof (需要 910B3 NPU)
# none      = 跳过性能验证
TRITON_AGENT_MSPROF_MODE=simulator

# 优化参数 (可选, 覆盖 config.py 默认值)
# TRITON_AGENT_MAX_ROUNDS=200
# TRITON_AGENT_TARGET_SPEEDUP=1.5
