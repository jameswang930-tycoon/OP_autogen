# opt-memory-bound / references

Memory-bound optimization techniques/guidelines go here — one markdown file per technique
(filled in the confidential env). Optional frontmatter may tag `applies_to: memory_underfilled`
and a `category`/scene. See `.claude/skills/extension-guide/references/README.md` for the tagging
convention. Empty here → loader returns nothing (no error).
