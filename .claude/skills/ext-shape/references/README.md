# ext-shape / references

Shape-transform / slice / block-pointer primitives (reshape / transpose / slice / block_ptr)
go here — one YAML per primitive, filled in the confidential env. Field contract identical to
extension-guide (name/semantics/signature/category/example/pitfalls + optional
module/applies_to); see the top-level `.claude/skills/extension-guide/references/README.md`.
