# ext-matmul / references

Matrix / convolution primitives (mmul / mmaddm / img2col — img2col is convolution-only) go
here — one YAML per primitive, filled in the confidential env. Field contract identical to
extension-guide (name/semantics/signature/category/example/pitfalls + optional
module/applies_to); see the top-level `.claude/skills/extension-guide/references/README.md`.
